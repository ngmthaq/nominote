import { memo } from "react";
import classes from "./style.module.scss";

const NoteBookPage = () => {
  return <div className={classes.noteBookPage}>NoteBookPage</div>;
};

export default memo(NoteBookPage);
