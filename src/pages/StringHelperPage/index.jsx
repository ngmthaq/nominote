import { memo } from "react";
import classes from "./style.module.scss";

const StringHelperPage = () => {
  return <div className={classes.stringHelperPage}>StringHelperPage</div>;
};

export default memo(StringHelperPage);
