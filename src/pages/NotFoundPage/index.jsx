import React, { memo } from "react";
import classes from "./style.module.scss";

const NotFoundPage = () => {
  return <div className={classes.notFoundPage}></div>;
};

export default memo(NotFoundPage);
