import { memo } from "react";
import classes from "./style.module.scss";

const CalendarPage = () => {
  return <div className={classes.calendarPage}>CalendarPage</div>;
};

export default memo(CalendarPage);
